# OA数据库四维矩阵分析报告

## 执行命令
```bash
python examples/analyze_oa_db.py --password "Tdsipass@@1234" --database oa
```

## 数据库概览

| 指标 | 数值 |
|------|------|
| 表数量 | 5 |
| 总列数 | 171 |
| 总数据量 | 3,256 行 |
| 总大小 | ~1.0 MB |

## 动态发现的业务域

### 主题域 Z=0: `mv` (4张表)
**描述**: 包含4张表，共2,442行数据，带时间戳

| 表名 | 生命周期 | 行数 | 列数 | 说明 |
|------|----------|------|------|------|
| mv_form_data_inst | mature | 814 | 12 | 表单数据实例 |
| mv_form_file | mature | 814 | 26 | 表单文件 |
| mv_formset_inst | legacy | 407 | 60 | 表单集实例（列数最多） |
| mv_opinion_inst | legacy | 407 | 24 | 意见实例 |

### 主题域 Z=1: `workitem` (1张表)
**描述**: 包含1张表，共814行数据，带时间戳

| 表名 | 生命周期 | 行数 | 列数 | 说明 |
|------|----------|------|------|------|
| mv_workitem | mature | 814 | 49 | 工作项 |

## 生命周期分布

```
mature: ███ (3张表) - 活跃使用
legacy: ██  (2张表) - 可能即将归档
```

## 四维矩阵坐标

| 表名 | Z(主题域) | X(生命周期) | Y(量级) | 颜色 |
|------|-----------|-------------|---------|------|
| mv_form_data_inst | 0 | 80 | 254 | #9a0808 (红色系) |
| mv_form_file | 0 | 80 | 254 | #9a2508 (红色系) |
| mv_formset_inst | 0 | 110 | 127 | #713e25 (棕色系) |
| mv_opinion_inst | 0 | 110 | 127 | #714525 (棕色系) |
| mv_workitem | 1 | 80 | 254 | #519a08 (绿色系) |

**颜色含义**:
- 红色系 (Z=0): `mv` 主题域（表单相关）
- 绿色系 (Z=1): `workitem` 主题域（工作项）
- 高亮度 (Y=254): 数据量较大（814行）
- 低亮度 (Y=127): 数据量较小（407行）
- 高饱和 (X=80): mature（成熟）
- 中饱和 (X=110): legacy（遗留）

## 质量评估

- **平均质量分**: 100.0/100
- **发现问题**: 0 个
- **所有表结构完整**，无警告

## 洞察与建议

### 1. 主题域划分
系统通过**命名相似度**自动发现：
- 4张以`mv_`开头的表归为`mv`域（表单相关）
- `mv_workitem`独立为`workitem`域

### 2. 生命周期判断
- `mv_formset_inst`和`mv_opinion_inst`被标记为**legacy**
  - 原因：数据量较小（407行），可能不再活跃
  - 建议：检查是否需要归档

### 3. 表复杂度
- **最复杂**: `mv_formset_inst` (60列)
- **最简单**: `mv_form_data_inst` (12列)

### 4. 数据分布
- 3张表有814行（活跃）
- 2张表有407行（可能是历史数据）

## 输出文件

| 文件 | 说明 |
|------|------|
| `oa_hypercube.json` | 可视化数据（用于Dash仪表盘） |
| `oa_analysis_report.json` | 完整分析报告（JSON格式） |

## 启动可视化

```bash
python -m hypercube.cli visualize --input oa_hypercube.json
```

访问 http://127.0.0.1:8050 查看交互式四维矩阵可视化。

## 技术说明

本次分析**完全没有预设任何业务域**，完全基于以下特征动态发现：
1. **命名相似度**: `mv_form_*` 表名聚类
2. **结构特征**: 都有时间戳列
3. **数据分布**: 自适应归一化

没有使用任何OA相关的硬编码逻辑，系统可应用于任意数据库。
